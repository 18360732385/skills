# Fixture: multi-host hooks

At least two hosts expose hooks signals (detect `S_HOOKS`):

- `.cursor/hooks.json`
- `.claude/settings.json` (`hooks`)
- `.qoder/settings.json` (`hooks`)
- `.trae/hooks.json`
- `.codebuddy/settings.json` (`hooks`)
