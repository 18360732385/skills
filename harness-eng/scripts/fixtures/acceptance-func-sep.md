# acceptance func fixture — separator must not trigger func-empty-semantics

### 服务类

`DemoService`

### 方法清单

| 方法签名 | 功能说明 | 参数 | 返回 |
|---|---|---|---|
| `run(String id)` | 按 id 执行演示逻辑并写审计 | id | void |
