#!/usr/bin/env node
/**
 * Intentionally stale consumer sync.mjs stub (Audit P0-1).
 * Missing current skill marker — freshness gate must fail.
 * HARNESS_SYNC_TMPL_ID: 0.5.0-stale
 * HARNESS_ENG_VERSION: 0.5.0
 */
console.error("stale fixture — do not run for real host sync");
process.exit(2);
