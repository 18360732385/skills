---
description: 数据库表结构同步规则
globs: {{GLOB_DB}}
alwaysApply: false
---

# 数据库表结构同步规则

## 权威来源说明

- **表结构真相（SSOT）**：`docs/db/table/` 下表文档与对应 `_change.sql`。
- **索引（非 SSOT）**：`docs/db/db.md` 仅作表导航；**禁止**在索引中展开完整建表语句。
- 冲突裁决：`docs/db/table/*.md` > `db.md`。
- 写真相前查看 `docs/db/templates/db-table-template.md`；写索引前查看 `db-index-template.md`。

## 文件命名（强制）

- 表真相：`NN-{table_name}.md`；变更 SQL：`NN-{table_name}_change.sql`（同序号）。
- 新建表取最大序号 +1，并更新 `db.md`、`table/README.md`。

## 强制检查流程

1. 先读 `docs/db/db.md`。
2. 再读 `docs/db/table/NN-{table}.md` 与 `_change.sql`。
3. **禁止**只读索引就改表文档或迁移。

## 判定清单（满足任一项 → 更新表真相 + `_change.sql`）

- 新建 / 删除表
- 改字段（名、类型、长度、可空、默认、注释）
- 增删改索引（含唯一 / 联合）
- 改主键或外键
- 数据订正 SQL（UPDATE / DELETE / INSERT）

另须更新 `db.md`：新建/删表或导航说明变更。纯追加迁移脚本（非建删表）一般**不**改索引里的版本区间说明。

## 变更触发条件

新建/改字段/改索引/主键外键/表注释/数据订正 SQL → 同步表文档；建表语句保持**最新完整 CREATE TABLE**。

## 同步操作（每次变更）

| 文件 | 操作 |
|---|---|
| `NN-{table}.md` | 更新变更记录；建表语句换成**变更后完整 CREATE TABLE** |
| `NN-{table}_change.sql` | 末尾追加本次 SQL（含注释：内容 / 操作人 / 时间） |
| `db.md` | 新建/删表或导航变更时更新 |

## 与功能资产 / API 联动

- 新建表 → 在对应 `docs/func/modules/*.md` 补数据实体（必要时改 `func.md`）
- 字段变更导致接口入出参变 → 联动 `docs/api/modules/`
- 表删除或字段废弃 → 在模块真相标注废弃与替代

## 门禁要求

- 变更说明含「表结构文档已同步」
- `.md` 与 `_change.sql` 同行提交；禁止只改索引漏更真相
- 迁移类翻车回流 `docs/agent-kb/pitfalls.md`

## 迁移模式（`db.md` 索引须声明）

- `flyway`：自动 / 半自动迁移；只追加新版本，禁改已发布脚本。推荐文件名：`V[0-9]{4}__(DDL|DML)__[a-z0-9_]+\.sql`；DDL/DML 分文件；下一号以目录最高号 +1 为准，**勿**把「当前最高号」写进 AGENTS/README 当硬编码上限。
- `manual_sql`：**无** Flyway 依赖（`spring.flyway.*` 不生效）；发版 / 新环境须**人工按序**执行迁移脚本；空库首迁以 `db.md` 声明的基线序列为准；`*_change.sql` 仅追溯 / 增量草稿，勿当首迁等价物；勿伪造 `flyway_schema_history`。
- `none`：无迁移脚本（表结构靠 `table/` 真相维护）。
