---
description: 后端 Java/yml 增量须打 SLF4J 步骤日志与中文注释（理解代码 + 日志定位）
globs: {{GLOB_OBSERVABILITY}}
alwaysApply: false
---

# 后端可观测与注释（日志 + 中文说明）

触碰后端 Java 或 `application*.yml` / `config/**/*.yml` 时适用。  
目标：**读代码能懂意图；看日志能跟步骤；失败可按 `traceId` / 业务键检索（含堆栈）**。

**本文件 = 日志与 Java/yml 注释行为细则 SSOT。**  
语言优先中文（若装 rule `14`）；踩坑回流见 `docs/agent-kb/pitfalls.md`（rule `19`）。

**增量-only**：只约束本次新增或改动的方法 / 字段 / 配置键。

---

## 一、SLF4J 步骤日志

若仓内有请求级收口日志（宽事件 / access log），二者并存：**收口 = 每请求一条指纹；本节 = Service / Tool / Infra 中间步骤**，不互相替代。

### 范围

- **要打**：`service/`、工具 / 插件层、`infrastructure/`、`scheduler/`、`handler/`
- Controller 成功路径交给请求收口日志；getter / Mapper / 纯委托不打步骤日志
- 本规则只管后端 SLF4J，不管前端 `console.*`

### 必须打点

| 场景 | 级别 | 至少带上 |
|---|---|---|
| `catch` / 未预期失败 | `error` | 业务键 + **Throwable 最后一参**（见下节） |
| 出进程 **硬失败**（HTTP 非 2xx、连接失败、重试耗尽、写库中止） | `error` | 目标标识 + Throwable 最后一参 |
| 可预期跳过、降级、可恢复重试 | `warn` | `reason=` 或等价键（可不带堆栈） |
| 改变控制流（abort / skip / fallback / 空结果早退） | `info` | 业务键（如 `orderId` / `taskCode` / `userId`） |
| 出进程 I/O **成功**（LLM、外部 HTTP、DB、锁） | `info` | 目标标识 + 结果摘要（条数 / 耗时 / 空否） |
| 批处理进度 | `info` | 已处理 / 总量（或本批） |

类上用 `@Slf4j`。`traceId` 若已在 MDC，消息里一般不必再写。

### error 与堆栈（问题定位硬约束）

ERROR 进错误日志；排障按 `traceId` 或业务关键字检索。**无 Throwable 则几乎无堆栈，定位失效。**

| 必须 | 禁止 |
|---|---|
| `log.error("…: key={}", val, ex)` —— **Throwable 放最后一参** | `log.error("… {}", e.getMessage())` / 只打 `e.toString()` 当唯一证据 |
| `catch` 后 return null / 空 / FAILED：**至少** `warn`/`error` | `catch (Exception ignored)` 静默吞掉 |
| 同一次失败：**一层**带阶段的 `error`（下层已打，收口层勿再打同义 error） | 多层对同一失败双打同义 `error` |
| 消息带业务键，便于 keyword 检索 | 密钥、Token、全文 prompt、文件字节、未截断大 JSON |

可预期降级、空结果、业务校验 → `warn` + `reason=`，**不要**抬成 `error` 刷屏。

### 写法

- 进度按批打 `info`（循环体内不打）
- 消息带业务键：`log.info("…: key={}", value)`（不用字符串拼接）
- 请求收口日志已含的 method / path / status，SLF4J 不复述

### 范本

```java
log.info("外部搜索调用: query={}, topN={}, userName={}", query, topN, userName);
log.warn("下游返回空文本: code={}, latencyMs={}", code, latencyMs);
// 正确：业务键 + Throwable 最后一参 → 错误日志有堆栈，可按关键字检索
log.error("写库失败: table={}", table, e);

// 错误：无堆栈，排障几乎无用
// log.error("写库失败: {}", e.getMessage());
```

---

## 二、中文注释（Java / yml）

便于后人理解意图与配置语义；与「只改本次触达行」一致。

| 触达物 | 必须写 | 写法 |
|---|---|---|
| 新增 / 大改 Java 类 | 类上方中文说明（职责 1～3 句） | 写职责，不复述类名 |
| 新增 / 改动方法（体 >5 行，或分支 / I/O 复杂） | 关键步骤中文注释；复杂入出参可一行说明 | 简单 getter / 一行委托不写 |
| 新增 / 改动 `*Properties` 字段 | 字段 `/** 中文 */` | 写用途，不写空英文占位 |
| 新增 / 改动 yml 键 | 键旁 `# 中文说明`（用途 / 默认 / 与 profile 覆盖关系） | 只改本次键；敏感键只写用途、不写明文 |

### 范本

```java
/** 图库连接与探测（demo.graph.*） */
@Data
@ConfigurationProperties(prefix = "demo.graph")
public class GraphProperties {
    /** 是否启用图库 */
    private boolean enabled = true;
}
```

```yaml
demo:
  graph:
    # 是否启用图库；false 时相关 Controller 不加载
    enabled: true
```

### 明确不纳入

- SQL 表 / 字段 `COMMENT` → db 文档同步规则（若启用 db 域）
- 前端 TS / React 注释 → 前端协作规则（若装）
- API / func 契约正文里的「说明」列 → 各文档同步规则
