#!/bin/sh
# harness-eng L3 soft commit gate (never blocks).
# Enable: git config core.hooksPath .githooks
DIR=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
if command -v node >/dev/null 2>&1; then
  node "$DIR/{{GITHOOKS_GATE_SCRIPT}}" --git || true
fi
exit 0
