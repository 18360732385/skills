---
description: harness SSOT 指针 — AGENTS + docs 契约优先
alwaysApply: true
enabled: true
---

# harness SSOT 指针（WorkBuddy）

以仓库根 `AGENTS.md` 与 `docs/func|api|db|redis` 为权威。本文件仅作入口提醒。

禁止写入明文密码；禁止用本规则覆盖契约真相。
