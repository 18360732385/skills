# {{CUSTOM_TOOL_LABEL}} — harness 入口适配

> **适配层（非 SSOT）**。由 harness-eng 按用户自定义工具面生成。  
> 工具 ID：`{{CUSTOM_TOOL_ID}}` · 本文件路径：`{{CUSTOM_ENTRY_PATH}}`

## 权威来源

1. 仓库根 `AGENTS.md` — Critical / Commands / Never do
2. `docs/func` → `docs/api` → `docs/db` → `docs/redis`（先索引再真相）
3. `docs/agent-kb/`（非契约）

## 说明

只放入口与优先级；**禁止**粘贴密码或他仓业务红线。

## 冲突裁决

磁盘最新 AGENTS / 契约真相 > 本适配文件 > 会话早期摘要。
