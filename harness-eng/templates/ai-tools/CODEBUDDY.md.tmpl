# {{REPO_NAME}} — WorkBuddy / CodeBuddy 入口

> **适配层（非 SSOT）**。完整命令与红线见 [`AGENTS.md`](./AGENTS.md)；契约见 `docs/func|api|db|redis`。

## 必读

1. `AGENTS.md`
2. 相关 `docs/**` 索引 → 真相
3. `.codebuddy/rules/` 中 harness 指针规则（若有）

勿在本文件写入密码或复制业务 Never do 清单。
