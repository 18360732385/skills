# {{REPO_NAME}} — WorkBuddy / CodeBuddy 入口

> **适配层（非 SSOT）**。完整命令与红线见 [`AGENTS.md`](./AGENTS.md)；契约见 `docs/func|api|db|redis|jobs`。

## 必读

1. `AGENTS.md`
2. 相关 `docs/**` 索引 → 真相
3. `.codebuddy/rules/` 中 harness 指针规则（若有）

本入口只作指针；**勿**在本文件粘贴明文密文或他仓业务 Never do 清单。
