# {{MODULE_NAME}} — AI 协作规范

> 本文件供 AI 在 `{{MODULE_DIR}}/` 下工作时遵循（**分册厚 SSOT**）。  
> 仓库命令与红线见根 [AGENTS.md](../AGENTS.md)。根文件不写本模块编码细则。

## 模块定位

| 项 | 说明 |
|---|---|
| 职责 | TODO(harness-eng): {{REPO_DESC}} |
| 边界 | TODO(harness-eng): 本模块做什么 / 不做什么 |
| 技术栈 | TODO(harness-eng) |
| 入口 / 端口 | TODO(harness-eng) |

## 动手前检查

1. 读根 AGENTS 与本分册
2. 改业务：`docs/func` 索引 → 真相
3. 改接口：`docs/api` 索引 → 真相
4. 改表 / Redis / Cron：对应 `docs/db` / `docs/redis` / `docs/jobs`（按已启用域）
5. 执行本模块构建 / 测试命令（见下）

## 常用命令

```text
TODO(harness-eng): 本模块构建 / 测试 / 运行命令（写可复制命令，勿只写「见 README」）
```

## 目录与职责

```text
TODO(harness-eng): 列出本模块关键目录（controller/service/… 或等价）与一句话职责
```

**分层约定**：TODO(harness-eng): 例如 Controller → Service → Infrastructure；编排优先在 Service。

## 编码约束（骨架）

- 统一响应 / 错误码：TODO(harness-eng) 按仓内现有类型
- 鉴权 / 用户上下文：TODO(harness-eng)；**禁止**硬编码 userId
- 日志与注释：若装 rule `21`，以其为准
- TODO(harness-eng): 本模块特有编码红线（一句话；细节 → Pn）

## 改动路径速查

| 需求 | 优先改动 |
|---|---|
| 新业务能力 | TODO(harness-eng): 包路径 + `docs/func/modules/…` |
| 新 / 改 REST | TODO(harness-eng): controller 路径 + `docs/api/modules/…` |
| 表结构 | TODO(harness-eng): migration / entity + `docs/db/table/…` |
| Redis Key | TODO(harness-eng): cache 包 + `docs/redis/keys/…` |
| 定时任务 | TODO(harness-eng): scheduler + `docs/jobs/tasks/…` |

## 禁止事项

> 一句话在此；细节回链 `docs/agent-kb/pitfalls.md` 的 `Pn`。

- 提交密钥
- 无文档同步地变更公开契约
- TODO(harness-eng): 域禁令 → Pn

## 文档同步门禁（交付必做）

| 变更类型 | 必须同步 |
|---|---|
| 业务能力 | `docs/func/modules/…` |
| REST / API | `docs/api/modules/…` |
| 表结构 | `docs/db/table/…` |
| Redis Key | `docs/redis/keys/…` |
| Cron / 定时任务 | `docs/jobs/tasks/…`（若启用 jobs） |
