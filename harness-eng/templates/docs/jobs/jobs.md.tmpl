# {{REPO_NAME}} 定时任务索引

> **本文件是索引，不是 SSOT。** 调度契约真相位于 [`tasks/`](./tasks/)。  
> 改 Cron / 增删任务 / 改开关前：先读本索引定位 task，再读对应真相；改后：更新对应 `tasks/NN-*.md`（新建/删任务时再改本导航）。

---

## 读文档说明

- **索引**：本文件（导航 + 全局约定）
- **真相**：`docs/jobs/tasks/NN-{slug}.md`（两位序号与下表顺序一致）
- **冲突裁决**：任务真相 > 本索引；**调度契约**以 jobs 为准，func/api 只保留业务与 REST，Cron 细节回链 jobs
- **禁止**在本文件展开单任务完整 Cron / 默认参数表（可保留错峰总览）
- 真相模板：[`templates/job-template.md`](./templates/job-template.md)
- 业务方法 / REST / Key / 表仍以 `docs/func|api|redis|db` 为 SSOT

---

## 全局约定

| 项 | 说明 |
|---|---|
| 调度框架 | TODO(harness-eng): 如 Spring `@Scheduled` |
| 执行器 | TODO(harness-eng): 统一执行器 / 锁 / 审计日志 |
| 多实例 | TODO(harness-eng): 分布式锁 Key 族（回链 redis） |
| 新增任务四件套 | 任务码枚举 · Scheduler · yml cron/enable · Metadata Registry · **本域真相** |

### 纪律（Never do）

- 只改 Cron 表达式却不同步 Registry / jobs 真相
- 在 Scheduler 内再加一层与执行器重复的锁
- 把业务方法清单 / OpenAPI 字段表写进 jobs 真相

---

## 任务文档

| # | 显示名 | task_code | 真相路径 | engine | mode |
|---|---|---|---|---|---|
| （待补充） | — | — | — | — | — |

详见 [tasks/README.md](./tasks/README.md)。

---

## 关联文档

| 文档类型 | 路径 |
|---|---|
| 功能资产 | `docs/func/modules/…`（回链） |
| API | `docs/api/modules/…`（回链） |
| Redis 锁 | `docs/redis/keys/…`（回链） |

## 变更记录（索引级）

| 版本 | 类型 | 内容 | 操作人 | 更新时间 |
|---|---|---|---|---|
| v0.1 | 创建 | harness-eng 初始化 jobs 索引骨架 | harness-eng | {{DATE}} |
