# 回链 — {{slug}}

> 产物回链总表；**仅控制器**（start / advance / close / handoff / proto-bridge / domain-bridge）维护；厨师只回报路径。close 时须补齐。  
> 目录：`docs/runs/active/{{slug}}/`（收口后在 `archive/`）。  
> **禁止**主题术语写入仓库根 `CONTEXT.md`。  
> 空指针写 `—`。

## 决策与术语
- ADR：—
- 术语增量（`术语增量.md` 或 Spec 术语小节）：—
- domain 桥：`skipped | skipped_by_user | entered` — —

## 规划
- Spec：—
- Plan：—
- tickets：—
- 会话存储（web+auth，O13）：`memory | sessionStorage | localStorage(+风险)` — —

## 原型（若 entered）
- 原型目录：—
- 交互说明：—
- 用户确认时间：—
- proto 桥：`skipped | skipped_by_user | entered` — —
- 轻量降级（O12，proto.skill null / controller_proxy）：`设计笔记.md` 草图+主路径 3 步 — —

## 测试（仅 F）
- 用例文档：`测试用例.md`
- 测试报告：`测试报告.md`
- 环境证据（verify）：—

## 仪式与降级
- chef_mode：`bound | controller_proxy` — —
- review_policy：`subagent | inline` — —（若 start 因无 Task 降级，写明「已降级 + 原因」）

## 硬闸授权（authorized_by）
> 合法取值仅：`user_chat` | `user_task_<id>` | `policy_exception`。  
> **禁止伪造聊天笔录**；批量/任务级授权用 `user_task_<id>`（如 `user_task_2026-09-21`）。  
> 不得把「看起来像对话」的假 transcript 当作授权证据。

| 闸 | 时间 | authorized_by | 备注 |
|---|---|---|---|
| triage | — | — | — |
| shared_understanding | — | — | — |
| design_confirmed | — | — | — |
| go | — | — | — |
| pre_impl | — | — | — |
| gate | — | — | — |
| verify | — | — | — |
| close | — | — | — |

## 同仓布局（M1 layout / packages）
> `layout: monorepo | multi_repo`（null≈multi_repo）。同仓 FE+BE 用 `packages`，**勿**用 sibling_repos 指向本仓包目录。  
> `docs_root` 默认 `docs/`（典礼落在仓库根，勿误放子包 docs）。

| layout | docs_root | packages (path / role) | 备注 |
|---|---|---|---|
| — | docs/ | — | — |

## 跨仓（O8 sibling_repos）
> 用户提到配对后端/前端时，progress.`sibling_repos` 至少一条；本表回链展示。  
> 本仓 role=web 且声明了 api sibling → Spec 须有「消费契约」小节或链接（见 gates-common）。  
> **M1**：`layout=monorepo` 时禁止 sibling_repos 指向与 `packages[].path` 同仓同路径。

| url | role (api\|web) | spec_path | 备注 |
|---|---|---|---|
| — | — | — | — |

## 其他
- env_verified：—
- env_notes（runtime / target / mismatch_reason / api_base_mode / pinned_deps / verify_commands / workdir_policy）：—
- integration_ready（O9：`cors_ready | proxy_ready | accepted_blocked`）：—
- L2 审核：`审核-<stage>.md` — —
- pending_invoke：—
- QA 报告：—
- 交接：—
- 跨仓前端提示：—
