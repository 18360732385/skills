# feature-eng 进度文件模板
# 复制为 docs/superpowers/runs/<slug>/progress.yaml；变量 {{slug}} {{title}} {{date}} 由 start 替换。

slug: "{{slug}}"
title: "{{title}}"
created_at: "{{date}}"
updated_at: "{{date}}"

# spike | bounded | full（分诊闸：用户确认后写入）
path: null

# triage | grill | design | domain | spec | plan | proto-bridge | proto | testdesign
# | pre-impl | implement | gate | verify | close | done
# 旁态（按需，不在主链）：smoke（B 路径冒烟）| diagnose（排障，先复现再改）
stage: triage

# proto 桥结果：skipped | skipped_by_user | entered（S 路径无此字段含义）
proto: null

artifacts:
  adr: []            # docs/adr/NNNN-*.md 路径列表
  spec: null         # docs/superpowers/specs/...
  plan: null         # docs/superpowers/plans/...
  proto: null        # 原型目录或文件
  testcases: null    # runs/<slug>/testcases.md（仅 F）
  test_report: null  # runs/<slug>/test-report.md（仅 F）
  qa_report: null    # 外部 QA 报告路径（可选）

# 来自 plan 的任务拆分；depends_on 为任务 id 列表
tasks: []
#  - id: T1
#    title: ...
#    depends_on: []
#    status: pending | doing | done

# 各闸通过时间（ISO）；无时间戳 = 未过
gates:
  triage: null
  shared_understanding: null
  design_confirmed: null
  go: null
  pre_impl: null
  gate: null
  verify: null
  close: null

# 用户显式接受的残留（verify 有 fail 仍 close 时记录）
accepted_residual: null
